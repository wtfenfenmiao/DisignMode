import quackbehavior.QuackBehavior;
import quackbehavior.silence;
import duck.Duck;
import duck.mallard;


public class main {

	public static void main(String[] args) {
		Duck amall=new mallard();
		amall.swim();
		amall.display();
		amall.makesound();
		amall.flying();
		
		QuackBehavior si=new silence();
		amall.changequack(si);
		amall.makesound();
	}

}
